# IntresseKlubben

Deployment URL: https://intresse-klubben.vercel.app/

- `backend/` – FastAPI + SQLAlchemy/Alembic, se `backend/README.md`
- `frontend/` – React + Vite, se `frontend/README.md`

Deploy: två separata Vercel-projekt (ett per mapp, `Root Directory` satt till `backend` respektive `frontend`) — se respektive README för miljövariabler.
